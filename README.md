# toolchain-builder
Standalone builder for LLVM/MLIR 19 (Allo-patched) + Allo wheel. Targets: macOS arm64 (macos-14) and Ubuntu 24.04 (x86_64). Includes a tiny CLI to print env exports and a script to fetch/setup artifacts for downstream repos (QREW, etc.).
